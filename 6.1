#include <iostream>
#include <cstring>
using namespace std;

int main(){
    char str[50];
    cin >> str;
    char str1[strlen(str)];       
            int i = strlen(str)-1;
            int j = 0;
            while (i >= 0)
            {

                str1[j] = str[i];
                --i;
                cout << str1[j];
                j++;
                    
            }

    return 0;
}
